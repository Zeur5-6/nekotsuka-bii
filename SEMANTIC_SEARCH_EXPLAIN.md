# セマンティック検索の追加について

## 何が変わったのか？

### 変更前（キーワード検索のみ）

**動作方式:**
- クエリを単語に分割
- ファイル名や内容にその単語が**文字列として**含まれているかチェック
- 単純なマッチング

**例:**
```
クエリ: 「エラーを処理する関数」
検索: 「エラー」「処理」「関数」という文字列を含むファイルを探す
結果: 「エラー」という文字列がないファイルは見つからない
```

### 変更後（セマンティック検索 + キーワード検索）

**動作方式:**
1. コードを数値ベクトル（意味を表す数値の配列）に変換
2. クエリも数値ベクトルに変換
3. ベクトル同士の距離（類似度）を計算
4. 意味が近いコードを検索結果として返す

**例:**
```
クエリ: 「エラーを処理する関数」
検索: 「例外を処理するメソッド」「エラーハンドリングをする関数」なども見つかる
結果: 意味が同じなら、使われている言葉が違っても見つけられる
```

## 実際の違い

### ケース1: 同じ意味だが違う言葉

**クエリ:** 「データベースに接続する処理」

**変更前（キーワード検索）:**
- ✅ `db_connect.py` （「データベース」と「接続」を含む）
- ❌ `sqlite_handler.py` （「SQLite」という単語はあるが「データベース」という単語がない）

**変更後（セマンティック検索）:**
- ✅ `db_connect.py` （「データベースに接続」）
- ✅ `sqlite_handler.py` （「SQLiteに接続」→ 意味が近いので見つかる！）

### ケース2: 曖昧なクエリ

**クエリ:** 「エラーを処理する関数」

**変更前:**
- 「エラー」「処理」「関数」という単語を含むファイルのみ

**変更後:**
- 「例外を処理する」「エラーハンドリングをする」「try-exceptで囲む」なども見つかる

## 技術的な変更点

### 追加されたもの

1. **`sentence-transformers`ライブラリ**
   - コードやクエリをベクトルに変換するモデル
   - 約420MB（初回ダウンロード、RAMに保存）

2. **ベクトル埋め込み機能**
   - コードファイルを500行ごとに分割
   - 各チャンクをベクトルに変換
   - メモリ（RAM）に保存

3. **ハイブリッド検索**
   - セマンティック検索 + キーワード検索を併用
   - セマンティック結果を優先、キーワード結果で補完

### メモリ使用量

- **VRAM:** 0GB（CPU推論のため）
- **RAM:** 約500MB〜1GB（モデル + ベクトルデータ）
- **現在の環境（VRAM 8GB, RAM 64GB）:** 問題なく動作

## 使い方

**自動で動作します！**
既存のコードはそのまま動作し、セマンティック検索が自動で有効になります。

```python
# 既存の使い方（そのまま動作）
code_reader = CodeReader()
results = code_reader.search_code("エラーを処理する関数")
# → セマンティック検索が自動で使用される
```

## インストール

```bash
pip install sentence-transformers
```

初回実行時にモデルが自動ダウンロードされます（約420MB）。

## 注意事項

- `sentence-transformers`がインストールされていない場合、自動でキーワード検索にフォールバック
- 初回のベクトル化には少し時間がかかる可能性がある（起動時に一度だけ）
- CPU推論のため、VRAMは使用しない